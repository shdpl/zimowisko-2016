# http://nixos.org/nixops/manual/
nixops create deployment.nix -d zimowisko
nixops list
nixops deploy -d zimowisko
nixops info -d zimowisko
nixops check -d zimowisko
nixops ssh -d zimowisko test1 uname -a
nixops show-physical -d zimowisko
nixops show-option -d zimowisko test1 system.nixosRelease
